--- C start ---

CodeSegment // 함수 코드 메소드
DataSegment // 상수 정적(static)변수 리터럴
Heap // 동적할당
Stack // 지역변수 매개변수

리눅스
# rpm -qa | grep ssh
# ps -ef | grep ssh

설정-네트워크-고급-포트포워딩-호스트/게스트 포트22 추가

rpm -qa | grep gcc // 설치 패키지 조회
rpm -qa | grep jdk
rpm -qa | grep python
rpm -qa | grep sqlite

rpm -qa | nl // 넘버라인 붙이기
rpm -qa | wc -l // 라인수만 알아보기

yum -y install gcc // 패키지 설치
yum -y install vim
yum -y install mariadb
yum -y install mariadb-server

gcc --version // 버전 확인 (ex. 4.8.5 20150623)

ctrl + L // 클리어

# mysql
# create database aDB;
# use aDB
# create table

