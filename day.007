= // 이항연산자

int i = 128; // 0000 0000 0000 0000 0000 0000 1000 0000
byte b = (byte) i; // b = -128

b = -128;
i = (int) b; // i = -128

Ctrl + / // 블록 주석/해제 (/ /)
Crtl + Shift + /,\ // 블록 주석/해제 (/* */)
Ctrl + D // 해당 줄 삭제

>>> // 부호까지 시프트

a = a+1
b = a++ // a=[a]+1; 
b = ++a // [a]=a+1;

a=1;
b=a++;
c=++a; // a=3, b=1, c=3
