ll /etc/ -Srh // S는 크기순 r은 역순 h는 1000이상 K를 붙여줌

cd - // 이전으로
cd ../.. // 2개 상위 폴더로
echo $? // 이전 명령어 실행 결과 (성공시 0, 실패시 0아닌값)

(application (shell (kernel) ) )

kernel // OS(운영체제), 메모리 관리, 시스템 관리
shell // 명령어 해석
application // 응용프로그램

bash // born again shell

enable // 내부명령어, bash
외부명령어, 패키지가 설치되어야함

